# Database (Managed PostgreSQL — Neon, Supabase, or Railway)
# Pooled connection string (used at runtime)
DATABASE_URL=
# Direct connection string (used for migrations only)
DIRECT_URL=

# Better Auth
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=

# Public app URL (auto-detected on Vercel if not set)
NEXT_PUBLIC_APP_URL=
